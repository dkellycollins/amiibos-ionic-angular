version: 0.2
            
phases:
  install:
    commands:
      - npm ci
  build:
    commands:
      - npm run build:prod
artifacts:
  files:
    - '**/*'
  name: amiibos-ionic-angular
  base-directory: 'www'
